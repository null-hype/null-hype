## 2024-05-23 - Avoid MathJax for Simple Notation
**Learning:** Using MathJax syntax (e.g., `$H_0$`) in GitHub Markdown triggers heavy rendering engine loads even for simple terms, slowing down page load.
**Action:** Replace simple mathematical notation with Unicode equivalents (e.g., `H₀`) in Markdown to improve render performance without sacrificing readability.
