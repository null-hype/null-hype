## 2026-02-25 - [GitHub Markdown Performance]
**Learning:** For documentation-only repositories (like GitHub profiles), avoiding MathJax syntax (`$H_0$`) in favor of Unicode (`H₀`) prevents loading the heavy MathJax rendering engine, improving page load speed.
**Action:** When working on GitHub profiles or READMEs, check for unnecessary MathJax usage and replace with Unicode where possible.
